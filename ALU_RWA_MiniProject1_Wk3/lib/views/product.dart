import 'package:flutter/material.dart';

class ProductCard extends StatelessWidget {
  var _type;
  var _imageUrl;
  var _price;

  ProductCard(this._type, this._imageUrl, this._price);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
